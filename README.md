# Word Garden

A small browser word puzzle game that runs as a static site.

## Play

This project is designed for GitHub Pages. After publishing, open the Pages URL on any device.

Progress and coins are saved in each browser with `localStorage`.

## Word Bank

The generated word bank contains 20,000 unique English words selected from the local `en_US` Hunspell dictionary available on the development machine.
